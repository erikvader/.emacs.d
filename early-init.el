;; NOTE: inhibit reading emacs.* entries from xresources. It is definitely reading Xft
;; settings from there anyways, so make sure those are set, otherwise the text in emacs
;; will look really bad.
(setq-default inhibit-x-resources t)
